module Basic.Trait.Bool

pub struct And {
  and : [E : Effect](Bool, () -> Bool ! E) -> Bool ! E
}

pub struct Or {
  or : [E : Effect](Bool, () -> Bool ! E) -> Bool ! E
}

pub struct Not {
  not : (Bool) -> Bool
}
