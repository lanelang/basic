module Basic.Trait.Applicative

import Basic.Trait.Mappable.{ Mappable }

pub struct Applicative[A : [Type] -> Type] {
  mappable_impl : Mappable[A]
  pure : [T](T) -> A[T]
  apply : [T, R, E : Effect](A[T], A[(T) -> R ! E]) -> A[R] ! E
}
